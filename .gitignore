# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# SAM build artifacts
.aws-sam/

# SAM deploy config — can contain account ID/region, keep local
samconfig.toml

# Editor
.vscode/

# Local secret files - never commit, values already live in AWS Secrets Manager
jira-webhook-secret.txt
secret-1.txt